export const staticVertex=`varying vec2 vUv;void main(){vUv=uv;gl_Position=projectionMatrix*modelViewMatrix*vec4(position,1.);}`;
export const staticFragment=`uniform sampler2D tDiffuse;uniform float time;uniform float amount;varying vec2 vUv;float hash(vec2 p){return fract(sin(dot(p,vec2(127.1,311.7))+time*13.)*43758.5453);}void main(){vec2 uv=vUv;float j=(hash(vec2(floor(uv.y*220.),time))-.5)*amount*.018;uv.x+=j;vec3 c=texture2D(tDiffuse,uv).rgb;float n=(hash(uv+time)-.5)*amount*.35;c+=n;c*=.82+.18*sin(uv.y*900.);float v=smoothstep(1.15,.25,length(uv-.5));gl_FragColor=vec4(c*v,1.);}`;